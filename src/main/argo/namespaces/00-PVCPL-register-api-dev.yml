kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: register-api-dev-pipeline-claim
  namespace: register-api-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ocs-storagecluster-ceph-rbd
  volumeMode: Filesystem
status:

